/*
 * Superseded and no longer imported.
 *
 * The capability mini-diagrams were removed: the group headings and item lists
 * communicate more than the sketches did. The platform architecture SVG was
 * replaced by a semantic layered stack in PlatformSection, which reflows and
 * reads correctly to assistive technology. Safe to delete.
 */
export {}
