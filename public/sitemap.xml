<?xml version="1.0" encoding="UTF-8"?>
<!--
  Not the source of truth.

  The published sitemap is generated at build time from `allRoutes` in
  src/lib/routes.ts (see the routeShells plugin in vite.config.ts) and
  overwrites this file in dist/, so a new page or case study cannot be
  left out of it.

  This copy exists only so `vite dev` and `vite preview` serve something at
  /sitemap.xml. Edit the route table, not this file.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://combine.se/</loc></url>
</urlset>
